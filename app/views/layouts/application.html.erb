<!DOCTYPE html>
<html>
  <head>
    <title>Klasse</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Hind:wght@400;600;700&family=M+PLUS+1:wght@400;500;700&display=swap" rel="stylesheet">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag "application" %>
    <%= stylesheet_pack_tag 'application' %>
  </head>
  <body class="bg-gradient-to-r from-blue-400 to-red-400 animate-gradient-x">
    <div
      id="vue-app"
      class="flex flex-col min-h-screen h-screen"
    >
      <the-navbar
        :user="<%= current_user ? serialize_resource(current_user) : 'null' %>"
        class="md:mb-0 mb-4"
      ></the-navbar>

      <%= yield %>
      <%= render partial: '/layouts/footer' %>
    </div>
  </body>
</html>
